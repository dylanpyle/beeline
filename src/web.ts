import lookupTable from "../data/words.lookup.json";
import findWords from "./find-words";

const form = document.querySelector<HTMLFormElement>(".beeline-form");

form.addEventListener("submit", function (event) {
  event.target.formData;
})();
